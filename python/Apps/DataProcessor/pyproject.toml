[project]
name = "data-processor"
version = "0.1.0"
description = "Pipeline and ports/adapters data ingestion service"
requires-python = ">=3.11"
dependencies = [
  "boto3",
  "pydantic>=2",
]

[tool.setuptools]
py-modules = []
