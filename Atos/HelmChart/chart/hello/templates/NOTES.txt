Release "{{ .Release.Name }}" deployed.

Echo message: {{ .Values.message }}
Replicas:     {{ .Values.replicaCount }}

From this demo setup, open:

  http://localhost:18080

Or port-forward manually:

  kubectl port-forward svc/{{ include "hello.fullname" . }} 18080:{{ .Values.service.port }}

Then:

  curl http://localhost:18080
