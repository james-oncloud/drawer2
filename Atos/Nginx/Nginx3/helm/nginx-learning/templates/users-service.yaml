apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-learning.usersServiceName" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    app: users-service
    {{- include "nginx-learning.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: users-service
  ports:
    - name: http
      port: {{ .Values.users.service.port }}
      targetPort: http
      protocol: TCP
