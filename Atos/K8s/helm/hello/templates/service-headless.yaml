apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello.fullname" . }}-headless
  labels:
    {{- include "hello.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello.selectorLabels" . | nindent 4 }}
